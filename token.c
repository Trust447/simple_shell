#include "main.h"
void *token()
{
/**	char *tok;
	char *d = "/n"
	char *cmd = get_cmd();
	char *cmd_cpy;

	cmd_cpy = malloc(sizeof (char*));

	 _strcpy(cmd_cpy, cmd);

	tok = strtok(cmd_cpy, d);

	printf("%s\n", tok);

	free(cmd_cpy);
	return (tok),
**/
}




